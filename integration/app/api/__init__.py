"""
api — REST API route modules.

Phase 2 will add:
  - /api/v1/customers — customer management endpoints
  - /api/v1/catalog   — product catalog endpoints
  - /api/v1/orders    — sales order endpoints
  - /api/v1/webhooks  — ERPNext and WhatsApp webhook receivers
"""
